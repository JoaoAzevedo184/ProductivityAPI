apiVersion: v1
kind: Service
metadata:
  name: {{ include "productivity-api.fullname" . }}
  labels:
    {{- include "productivity-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "productivity-api.selectorLabels" . | nindent 4 }}